cluster:
  installed: true
  name: one
  cloud:
    provider: aws
    region: eu-central-1
    vpc: default
    domain: shalb.net
    provisioner:
      type: minikube
      instanceType: m5.large
  apps:
    - /kubernetes/apps/logging
