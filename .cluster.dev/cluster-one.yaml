cluster:
  installed: true
  name: minikube-nine
  cloud:
    provider: aws
    region: eu-central-1
    vpc: default
    domain: aws.shalb.com
  provisioner:
    type: minikube
    instanceType: m5.large
  apps:
    - /kubernetes/apps/logging
